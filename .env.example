# Infura API key — used to construct RPC URLs for all chains
INFURA_API_KEY=your-infura-api-key

# Pimlico API key — used for ERC-4337 bundler/paymaster (optional)
PIMLICO_API_KEY=your-pimlico-api-key

# Discord webhook for daily reports (optional, used by CI)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
